import { NotMainAssetsPipe } from './not-main-assets.pipe';

describe('NotMainAssetsPipe', () => {
  it('create an instance', () => {
    const pipe = new NotMainAssetsPipe();
    expect(pipe).toBeTruthy();
  });
});
