import { MainAssetPipe } from './main-asset.pipe';

describe('MainAssetPipe', () => {
  it('create an instance', () => {
    const pipe = new MainAssetPipe();
    expect(pipe).toBeTruthy();
  });
});
