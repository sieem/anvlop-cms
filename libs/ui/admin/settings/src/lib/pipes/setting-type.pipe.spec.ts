import { SettingTypePipe } from './setting-type.pipe';

describe('SettingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SettingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
