import { SlugDirective } from './slug.directive';

describe('SlugDirective', () => {
  it('should create an instance', () => {
    const directive = new SlugDirective();
    expect(directive).toBeTruthy();
  });
});
